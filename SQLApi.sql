USE sqlApi;


CREATE TABLE SqlApiTable (
     ID int PRIMARY KEY IDENTITY(1,1),
     cca2 varchar(50),
     status varchar(60),
     region varchar(60),
     subregion varchar(60),
);

SELECT * FROM sqlApi;
